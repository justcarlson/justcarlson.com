---
phase: 05-personal-brand-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/consts.ts
  - src/constants.ts
  - src/components/Sidebar.astro
autonomous: true

must_haves:
  truths:
    - "Social link titles display 'Justin Carlson on GitHub' (person name)"
    - "Site title displays 'justcarlson' (brand name)"
    - "Gravatar URL includes email hash for real profile image"
  artifacts:
    - path: "src/consts.ts"
      provides: "SITE.authorFullName field for person name contexts"
      contains: "authorFullName"
    - path: "src/constants.ts"
      provides: "Social link titles using person name"
      contains: "authorFullName"
    - path: "src/components/Sidebar.astro"
      provides: "Gravatar avatar with email hash"
      pattern: "gravatar.com/avatar/[a-f0-9]+"
  key_links:
    - from: "src/constants.ts"
      to: "src/consts.ts"
      via: "SITE.authorFullName import"
      pattern: "SITE\\.authorFullName"
---

<objective>
Add person name vs brand name distinction to configuration and update components to use contextual naming.

Purpose: Fix the disconnect between "justcarlson" (brand) appearing in social link titles vs "Justin Carlson" (person) on external profiles
Output: Config with authorFullName, social links using person name, Gravatar with proper hash
</objective>

<execution_context>
@/home/jc/.claude/get-shit-done/workflows/execute-plan.md
@/home/jc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-personal-brand-cleanup/05-RESEARCH.md

@src/consts.ts
@src/constants.ts
@src/components/Sidebar.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add authorFullName to SITE configuration</name>
  <files>src/consts.ts</files>
  <action>
Update the Site interface and SITE object in src/consts.ts:

1. Add `authorFullName: string;` to the Site interface after `author: string;`
2. Add `authorFullName: "Justin Carlson",` to the SITE object after `author: "justcarlson",`
3. Update the `author` field value from "Just Carlson" to "justcarlson" (brand name, lowercase)
4. Update the `title` field value from "Just Carlson" to "justcarlson" (brand name)

This separates:
- `SITE.author` / `SITE.title` = "justcarlson" (brand name for site identity)
- `SITE.authorFullName` = "Justin Carlson" (person name for social contexts)
  </action>
  <verify>grep -E "author:|authorFullName:" src/consts.ts shows both fields with correct values</verify>
  <done>SITE has author="justcarlson" and authorFullName="Justin Carlson"</done>
</task>

<task type="auto">
  <name>Task 2: Update social link titles to use person name</name>
  <files>src/constants.ts</files>
  <action>
Update src/constants.ts SOCIALS array to use authorFullName for link titles:

1. At the top, add authorFullName to the SITE import: `import { SITE } from "./consts";`
   - Check if authorFullName is already destructured, if not update the import
2. Update the GitHub entry linkTitle from `${SITE.title} on Github` to `${SITE.authorFullName} on Github`
3. Update the LinkedIn entry linkTitle from `${SITE.title} on LinkedIn` to `${SITE.authorFullName} on LinkedIn`

This makes social links say "Justin Carlson on GitHub" instead of "justcarlson on Github".
  </action>
  <verify>grep "authorFullName" src/constants.ts shows the field being used in linkTitle</verify>
  <done>SOCIALS linkTitle values reference SITE.authorFullName for person name context</done>
</task>

<task type="auto">
  <name>Task 3: Update Gravatar URL with email hash</name>
  <files>src/components/Sidebar.astro</files>
  <action>
Update Sidebar.astro to use a proper Gravatar hash:

1. Ask user for their Gravatar email MD5 hash (they should generate offline to keep email private)
   - If hash not provided in conversation, use placeholder: `[GRAVATAR_HASH]`
   - Add comment: `// Replace [GRAVATAR_HASH] with MD5 hash of your Gravatar email`

2. Update the Gravatar URL in the img src from:
   `src="https://gravatar.com/avatar/?s=240&d=mp"`
   to:
   `src="https://gravatar.com/avatar/{hash}?s=240&d=mp"`

3. Update the alt text to use SITE.authorFullName if available:
   `alt={SITE.authorFullName + "'s avatar"}` or `alt={SITE.author + "'s avatar"}` if authorFullName not imported

Note: The user should provide their Gravatar email hash. If not provided, leave as placeholder with clear instructions.
  </action>
  <verify>grep "gravatar.com/avatar" src/components/Sidebar.astro shows a hash or placeholder in the URL</verify>
  <done>Sidebar avatar uses Gravatar URL with hash (or documented placeholder)</done>
</task>

</tasks>

<verification>
1. Run `npm run build` to verify no TypeScript errors
2. Run `npm run dev` and check:
   - Sidebar social links have correct hover text (Justin Carlson on GitHub)
   - Site title shows "justcarlson"
   - Avatar image loads from Gravatar (or shows mystery person if placeholder)
3. Verify config: `grep -E "author|title" src/consts.ts` shows correct values
</verification>

<success_criteria>
1. SITE.author = "justcarlson" (brand)
2. SITE.authorFullName = "Justin Carlson" (person)
3. SITE.title = "justcarlson" (brand)
4. SOCIALS linkTitle values use authorFullName
5. Gravatar URL has hash or documented placeholder
6. Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-personal-brand-cleanup/05-01-SUMMARY.md`
</output>
